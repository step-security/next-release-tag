{
  "name": "next-release-tag",
  "version": "6.5.0",
  "description": "Github Action to generate next Release Tag",
  "author": "step-security",
  "license": "MIT",
  "homepage": "https://github.com/step-security/next-release-tag#readme",
  "scripts": {
    "build": "npm run clean && ncc build src/index.ts --minify --license licenses.txt",
    "clean": "rimraf dist/",
    "all": "npm run lint && npm test && npm run build",
    "lint": "xo",
    "test": "vitest run"
  },
  "dependencies": {
    "@actions/core": "3.0.0",
    "@actions/github": "9.0.0",
    "axios": "^1.18.1"
  },
  "devDependencies": {
    "@types/node": "^26.1.1",
    "@vercel/ncc": "0.44.0",
    "rimraf": "6.1.3",
    "tsx": "^4.23.0",
    "typescript": "5.9.3",
    "vitest": "^4.1.10",
    "xo": "^4.0.0"
  }
}
